@{
  Layout = "_Layout";
}
<p>Students</p>

@if (Model.Count == 0)
{
 <h4>There are no students registered</h4>
}

@foreach(var student in Model)
{
  <h4><a href = "/students/@student.id">@student.name</a> was enrolled on @student.date</h4>
}

<a href = "/students/new">Add a new Student</a>
